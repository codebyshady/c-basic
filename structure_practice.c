#include <stdio.h>
#include <string.h>
// The struct thing start here >>>
struct c
{
    int no;
    char name[50];
    double salary;
};
// struct name distrubution >>>
struct c x1, x2, x3;
int main()
{typedef(printf("The serial entered for new user is :%d\n Now enter the name:\n",x2.no);,new);
    x1.no = 1;
    strcpy(x1.name,"Rahul");
    x1.salary=450000.23;
    printf("The serial no. for the first term is %d\n The Name of First member is %s \n salary is %lf\n", x1.no,x1.name,x1.salary);
    // Input of X2.stuct ...
    printf("Enter serial no. for next user:");
    scanf("%d",&x2.no);
    printf("The serial entered for new user is :%d\n Now enter the name:\n",x2.no);
    scanf("%s",&x2.name);
    printf ("The name entered for next user is:%s",x2.name);
    new;
    return 0;

}